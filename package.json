{
  "name": "@hdx/desktop",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev:web": "vite --host 127.0.0.1 --port 1420 --strictPort",
    "build:web": "vite build",
    "dev:full": "tauri dev --config src-tauri/tauri.full.conf.json --features flavor-full",
    "dev:online": "tauri dev --config src-tauri/tauri.online.conf.json --features flavor-online",
    "build:full": "tauri build --config src-tauri/tauri.full.conf.json --features flavor-full",
    "build:online": "tauri build --config src-tauri/tauri.online.conf.json --features flavor-online",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "typescript": "^5.8.0",
    "vite": "^8.0.8"
  }
}
